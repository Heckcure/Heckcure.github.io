source "https://rubygems.org"

# Pins the exact Jekyll + plugin versions GitHub Pages builds with,
# so `bundle exec jekyll serve` matches the live site.
gem "github-pages", group: :jekyll_plugins

gem "webrick"
